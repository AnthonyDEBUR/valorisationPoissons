% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_puissance_pechee_histogram.R
\name{plot_puissance_pechee_histogram}
\alias{plot_puissance_pechee_histogram}
\title{Histogramme des puissances de pêche ASPE pour une station ou un CPP}
\usage{
plot_puissance_pechee_histogram(
  yaml_path,
  code_station = NULL,
  code_point_prelevement_aspe = NULL,
  annee_debut = 1950,
  annee_fin = as.integer(format(Sys.Date(), "\%Y")),
  n_last = 10,
  titre = "Puissance (kVA)",
  schema = "qe",
  exclure_typologies = c("Incorrecte")
)
}
\arguments{
\item{yaml_path}{Chemin vers le fichier YAML de connexion PostgreSQL.}

\item{code_station}{Code Sandre de la station (prioritaire si fourni).}

\item{code_point_prelevement_aspe}{Code du point ASPE utilisé si \code{code_station} est \code{NULL}.}

\item{annee_debut}{Année minimale (défaut : 1950).}

\item{annee_fin}{Année maximale (défaut : année courante).}

\item{n_last}{Nombre de dernières dates distinctes à afficher (défaut : 10).}

\item{titre}{Titre du graphique (défaut : \code{"Puissance (kVA)"}).}

\item{schema}{Schéma SQL contenant les tables (défaut \code{"qe"}).}

\item{exclure_typologies}{Vecteur des typologies d'opération à exclure
(défaut : \code{"Incorrecte"}).}
}
\value{
Un ggplot2 ou \code{NULL} si aucune donnée.
}
\description{
Extrait les puissances de pêche (\code{puissance}) depuis la table
\code{aspe_operations}, filtrées selon :
\itemize{
\item une station (\code{code_station}) \strong{ou} un point ASPE (\code{code_point_prelevement_aspe}) ;
\item un intervalle d'années (\code{annee_debut} – \code{annee_fin}) ;
\item un filtre d'exclusion de typologies d'opérations via
\code{libelle_qualification_operation} (\code{exclure_typologies}) ;
\item les \code{n_last} dernières dates distinctes.
}
}
\details{
Produit un histogramme ggplot2 où chaque barre représente la \strong{puissance (kVA)}.
Les dates sont affichées en ordre chronologique (format \verb{\%d/\%m/\%y}), pivotées
de 90° pour lisibilité.
}
