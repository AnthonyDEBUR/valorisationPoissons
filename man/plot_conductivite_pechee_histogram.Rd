% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_conductivite_pechee_histogram.R
\name{plot_conductivite_pechee_histogram}
\alias{plot_conductivite_pechee_histogram}
\title{Histogramme des conductivités ASPE pour une station ou un CPP}
\usage{
plot_conductivite_pechee_histogram(
  yaml_path,
  code_station = NULL,
  code_point_prelevement_aspe = NULL,
  annee_debut = 1950,
  annee_fin = as.integer(format(Sys.Date(), "\%Y")),
  n_last = 10,
  titre = "Conductivité (µS/cm)",
  schema = "qe",
  exclure_typologies = c("Incorrecte")
)
}
\arguments{
\item{yaml_path}{Chemin vers le fichier YAML de connexion PostgreSQL.}

\item{code_station}{Code Sandre (prioritaire si fourni).}

\item{code_point_prelevement_aspe}{Code du point ASPE (si \code{code_station} est NULL).}

\item{annee_debut}{Année minimale (défaut = 1950).}

\item{annee_fin}{Année maximale (défaut = année courante).}

\item{n_last}{Nombre de dernières dates distinctes à afficher (défaut = 10).}

\item{titre}{Titre du graphique (défaut \code{"Conductivité (µS/cm)"}).}

\item{schema}{Schéma SQL (défaut \code{"qe"}).}

\item{exclure_typologies}{Vecteur des typologies d’opérations à exclure
(défaut \code{"Incorrecte"}).}
}
\value{
Un ggplot2 ou \code{NULL} si aucune donnée.
}
\description{
Extrait les conductivités instantanées (\code{conductivite}) depuis la table
\code{aspe_operations}, filtrées selon :
\itemize{
\item une station (\code{code_station}) \strong{ou} un point ASPE (\code{code_point_prelevement_aspe}) ;
\item un intervalle d'années (\code{annee_debut} – \code{annee_fin}) ;
\item un filtre d'exclusion de typologies via le champ
\code{libelle_qualification_operation} (\code{exclure_typologies}) ;
\item les \code{n_last} dernières dates distinctes.
}
}
\details{
Produit un histogramme ggplot2 où chaque barre représente la conductivité
(µS/cm), avec dates ordonnées chronologiquement et pivotées à 90°.
}
