% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_surface_pechee_histogram.R
\name{plot_surface_pechee_histogram}
\alias{plot_surface_pechee_histogram}
\title{Histogramme des surfaces de pêche ASPE pour une station ou un CPP}
\usage{
plot_surface_pechee_histogram(
  yaml_path,
  code_station = NULL,
  code_point_prelevement_aspe = NULL,
  annee_debut = 1950,
  annee_fin = as.integer(format(Sys.Date(), "\%Y")),
  n_last = 10,
  titre = "Surface pêchée en m²",
  schema = "qe",
  exclure_typologies = c("Incorrecte")
)
}
\arguments{
\item{yaml_path}{Chemin YAML (connexion PostgreSQL).}

\item{code_station}{Code Sandre de la station (prioritaire si fourni).}

\item{code_point_prelevement_aspe}{Code du point ASPE (utilisé si \code{code_station} est NULL).}

\item{annee_debut}{Année minimale (défaut 1950).}

\item{annee_fin}{Année maximale (défaut = année courante).}

\item{n_last}{Nombre de dernières dates à afficher (défaut 10).}

\item{titre}{Titre du graphique (défaut \code{"Surface pêchée en m²"}).}

\item{schema}{Schéma SQL contenant \code{aspe_operations} (défaut \code{"qe"}).}

\item{exclure_typologies}{Vecteur de typologies d’opération à \strong{exclure}
(défaut \code{"Incorrecte"}).}
}
\value{
Un objet ggplot2 ou \code{NULL} si aucune donnée.
}
\description{
Extrait les surfaces pêchées (\code{surface_calculee}) depuis la table
\code{aspe_operations} après filtrage :
\itemize{
\item par station (\code{code_station}) OU par point ASPE (\code{code_point_prelevement_aspe}) ;
\item par période (\code{annee_debut} – \code{annee_fin}) ;
\item par exclusion des typologies d’opération (\code{libelle_qualification_operation})
via l’argument \code{exclure_typologies} ;
\item par les \code{n_last} dernières dates distinctes.
}
}
\details{
Retourne un histogramme ggplot2 où chaque barre représente la surface de pêche
(en m²), avec des dates chronologiques pivotées à 90° pour améliorer la lisibilité.
}
